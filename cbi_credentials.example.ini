# ==============================================================================
# CBI DATABASE CREDENTIALS — TEMPLATE
#
# 1. Copy this file to "cbi_credentials.ini" (same folder) — that exact
#    filename is what run_cbi_pipeline.ps1 and setup_migration.ps1 look for.
# 2. Fill in your real values below.
# 3. Never commit cbi_credentials.ini to git. See SECURITY NOTE at the
#    bottom of README.md — .gitignore is already set up to block it, but
#    double-check with `git status` before any `git add .` in this folder.
# ==============================================================================

[postgresql]
host=localhost
port=5432
dbname=CBI
user=postgres
password=REPLACE_WITH_YOUR_PASSWORD
